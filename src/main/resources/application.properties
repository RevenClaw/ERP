# --- Auth database (credentials for users, roles, password hashes) ---
auth.datasource.jdbcUrl=jdbc:mysql://localhost:3306/univ_auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
auth.datasource.username=auth_user
auth.datasource.password=auth_pass123
auth.datasource.maximumPoolSize=5

# --- ERP database (courses, sections, enrollments, grades) ---
erp.datasource.jdbcUrl=jdbc:mysql://localhost:3306/univ_erp?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
erp.datasource.username=erp_user
erp.datasource.password=erp_pass123
erp.datasource.maximumPoolSize=10

# --- General application flags ---
app.maintenance.bannerText=System in maintenance mode. Changes are temporarily disabled.

